#!/usr/bin/env bash
# {{ ansible_managed }}
#
# {{ firefox_edition.name }}

if [[ "${XDG_SESSION_TYPE}" == "wayland" ]]; then
  export MOZ_ENABLE_WAYLAND=1
fi

firejail /usr/bin/{{ firefox_edition.pkg }}

# vim: set syntax=bash
