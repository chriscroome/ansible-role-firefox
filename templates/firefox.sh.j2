#!/usr/bin/env bash
# {{ ansible_managed }}

if [[ "${XDG_SESSION_TYPE}" == "wayland" ]]; then
  export MOZ_ENABLE_WAYLAND=1
fi

firejail /usr/lib/{{ firefox_edition.pkg }}/firefox

# vim: set ft=bash:
